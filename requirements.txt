requests>=2.31
pyyaml>=6.0
apscheduler>=3.10

# OpenTelemetry core
opentelemetry-api>=1.25
opentelemetry-sdk>=1.25
opentelemetry-semantic-conventions>=0.46b0

# OTLP exporters (gRPC)
opentelemetry-exporter-otlp-proto-grpc>=1.25

# OTLP exporters (HTTP — fallback)
opentelemetry-exporter-otlp-proto-http>=1.25

# Protobuf (necessário para serialização OTLP)
protobuf>=4.25

# gRPC runtime
grpcio>=1.62
